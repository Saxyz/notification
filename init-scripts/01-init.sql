-- Archivo de ejemplo para inicialización de la base de datos
-- Este script se ejecuta automáticamente cuando se crea el contenedor de PostgreSQL

-- Crear extensiones útiles (opcional)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Crear índices adicionales o configuraciones específicas (opcional)
-- Ejemplo:
-- CREATE INDEX IF NOT EXISTS idx_notifications_created_at ON notifications(created_at);

-- Este archivo es opcional y puede eliminarse si no necesitas scripts de inicialización